#include <iostream>
using namespace std;

int main() {
	int t, n, m, k;
	cin >> t;
	while(t){
	    cin >> n >> m >>k;
	    if(n%k==0 && m>=(n/k))
	        cout << "yes\n";
	    else if(n%k!=0 && m>(n/k))
	        cout << "yes\n";
	    else
	        cout << "no\n";
	    t--;
	}
	return 0;
}
